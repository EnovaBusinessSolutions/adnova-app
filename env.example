# Example env file for local development

# Core
PORT=3000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/adnova_local
SESSION_SECRET=replace_with_random_string

# OpenAI
OPENAI_API_KEY=sk-...

# ADRAY server info (used by plugin if hardcoded in plugin settings)
EXAMPLE_SERVER_URL=http://localhost:3000

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
