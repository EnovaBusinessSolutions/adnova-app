// public/script.js

document.addEventListener('DOMContentLoaded', () => {
  const page = document.body.dataset.page;
  // Aquí va solo lógica global que no sea login.
  // Por ejemplo:
  // if (page === 'dashboard') initDashboardMenus();
});
