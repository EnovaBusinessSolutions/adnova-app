services:
  - type: web
    name: adnova-ai
    env: node
    buildCommand: npm install && npm run build:landing && npx prisma generate --schema=backend/prisma/schema.prisma
    startCommand: npm run prisma:push && node backend/index.js
    envVars:
      - key: NODE_VERSION
        value: 20
