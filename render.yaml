services:
  - type: web
    name: adnova-ai
    env: node
    buildCommand: npm install --legacy-peer-deps
    startCommand: node backend/index.js
    envVars:
      - key: NODE_VERSION
        value: 18
