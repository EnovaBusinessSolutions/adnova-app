services:
  - type: web
    name: adnova-ai
    env: node
    branch: german/dev
    autoDeploy: true
    buildCommand: npm ci && npm run build:landing && npm run build:dashboard && npx prisma generate --schema=backend/prisma/schema.prisma
    startCommand: node backend/scripts/migrate-clarity-columns.js && npm run prisma:push && node backend/scripts/backfill-clarity-urls.js && npm run db:backfill:layer45 && node backend/index.js
    envVars:
      - key: NODE_VERSION
        value: 20

  - type: worker
    name: adnova-ai-mcp-worker
    env: node
    branch: german/dev
    autoDeploy: true
    buildCommand: npm ci
    startCommand: npm run worker:mcp
    envVars:
      - key: NODE_VERSION
        value: 20
